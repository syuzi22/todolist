
// {[todoId]: todoText}
const reducer = (state = {}, action) => {
    const { type, payload } = action;
    switch (type) {
        // case SOMETHING:
        //   return ......
        default:
          return state;
    }
};

export default reducer;